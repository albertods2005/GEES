<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl text-gray-800">Multas</h2></x-slot>
<div class="py-6 max-w-7xl mx-auto sm:px-6 lg:px-8">@include('gees.partials.mensajes')
<x-admin-filter placeholder="Buscar por ID, equipo, jugador, motivo, importe o fecha" create-route="multas.create" create-label="Nueva multa" />
<div class="overflow-x-auto bg-white rounded shadow"><table class="min-w-full text-sm"><thead class="bg-gray-100"><tr><th class="p-2">ID</th><th class="p-2">Equipo</th><th class="p-2">Jugador</th><th class="p-2">Motivo</th><th class="p-2">Monto</th><th class="p-2">Pagada</th><th class="p-2">Acciones</th></tr></thead><tbody>@foreach($multas as $multa)<tr class="border-t"><td class="p-2">{{ $multa->id_multa }}</td><td class="p-2">{{ $multa->equipo?->nombre_equipo }}</td><td class="p-2">{{ $multa->nombre_jugador }}</td><td class="p-2">{{ $multa->motivo }}</td><td class="p-2">{{ $multa->monto }}</td><td class="p-2">{{ $multa->pagada ? 'Sí' : 'No' }}</td><td class="p-2"><a class="text-blue-600" href="{{ route('multas.edit',$multa) }}">Editar</a><form method="POST" action="{{ route('multas.destroy',$multa) }}" class="inline">@csrf @method('DELETE')<button class="text-red-600 ms-2" onclick="return confirm('¿Eliminar multa?')">Eliminar</button></form></td></tr>@endforeach</tbody></table></div>
<div class="mt-4">{{ $multas->links() }}</div></div></x-app-layout>
