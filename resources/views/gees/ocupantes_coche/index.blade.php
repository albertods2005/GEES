<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl text-gray-800">Ocupantes Coche</h2></x-slot>
<div class="py-6 max-w-7xl mx-auto sm:px-6 lg:px-8">@include('gees.partials.mensajes')
<a href="{{ route('ocupantes-coche.create') }}" class="mb-4 inline-block rounded bg-blue-600 px-4 py-2 text-white">Nuevo ocupante</a>
<div class="overflow-x-auto bg-white rounded shadow"><table class="min-w-full text-sm"><thead class="bg-gray-100"><tr><th class="p-2">ID</th><th class="p-2">Coche</th><th class="p-2">Ocupante</th><th class="p-2">Acciones</th></tr></thead><tbody>@foreach($ocupantes as $ocupante)<tr class="border-t"><td class="p-2">{{ $ocupante->id }}</td><td class="p-2">{{ $ocupante->id_coche }}</td><td class="p-2">{{ $ocupante->nombre_ocupante }}</td><td class="p-2"><a class="text-blue-600" href="{{ route('ocupantes-coche.edit',$ocupante) }}">Editar</a><form method="POST" action="{{ route('ocupantes-coche.destroy',$ocupante) }}" class="inline">@csrf @method('DELETE')<button class="text-red-600 ms-2" onclick="return confirm('¿Eliminar ocupante?')">Eliminar</button></form></td></tr>@endforeach</tbody></table></div>
<div class="mt-4">{{ $ocupantes->links() }}</div></div></x-app-layout>
